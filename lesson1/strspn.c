#include <stdio.h>
#include <string.h>

unsigned int _strspn(char * s, char *accept)
{
   unsigned int i,j;
    for( i =0; s[i] = '\0'; i++)
    {
        for(j = 0; accept[j] != s[j]; j++)
        {
           if( accept = '\0');
            return(i);
        }   
       
    }
     return(i);
}